# Historical MCP Endpoint Record

**Status: not an active deployment runbook.**

The current checkout contains no MCP proxy or Onyx/DeerFlow deployment under
`deployment/`, and no Compose file exposes shared MCP endpoints. Do not use
previous `localhost:8095`, `onyx-mcp-proxy`, Scite, Consensus, or Onyx MCP
instructions as current operational configuration.

This filename is retained so historical links and decision records remain
resolvable. Any future MCP integration must be introduced as a project runner
or shared adapter, then documented with its actual configuration, ownership,
authentication, and health checks.
