# Platform Backlog

Use this file for operational work on Onyx, DeerFlow, MCP, Docker, models, and deployment security. This is the single operational source of truth for the repository. Keep `ACTION_PLAN.md` high level, keep `docs/archive/` historical only, and do not put Robert's scientific claims or fit conclusions here.

| Priority | System | Issue | Why It Matters | Next Action | Status |
|---|---|---|---|---|---|
| P1 | Onyx | Active document set still has incomplete baseline and citation-context coverage | Retrieval quality is still bounded by what `HEP Phenomenology References` can ground | Split seeded sets were created and attached; HEP-native arXiv/INSPIRE/HEPData source grounding plus minimal Tsallis and Blast-Wave baseline literature are now seeded and verified for `RAG-12` through `RAG-14`; next close the remaining small-system radial-flow gap for `RAG-15` and the citation-context gap for `RAG-19` | Partially done |
| P1 | Onyx | OpenSearch retrieval cutover is not ready for the current tenant | `opensearch_document_migration_record` still has 0 rows, the tenant bookkeeping remains `PENDING`, and the active alt index is still chunk-incomplete; flipping retrieval now would move the tenant onto an unreconciled copy | Fix in dependency order: (1) keep the LiteLLM contextual-summary timeout fix in place until rebuild completions are stable; (2) [DONE] keep the `KeyError: 'document_id'` transformer fix deployed; (3) rerun the full rebuild sweep for connector/credential pairs `(11,0)`, `(13,8)`, `(14,9)`, `(10,6)`, `(1,1)`, `(7,5)`, `(4,3)`, and `(3,2)`; (4) verify `opensearch_document_migration_record` population and chunk parity via `deployment/helper/onyx_opensearch_cutover.py --json`; (5) only then flip tenant `enable_opensearch_retrieval` | Blocked (`opensearch_document_migration_record` still has 0 rows; active alt index still had 37 chunk-count mismatches across 65 docs on `2026-04-28`; durable Vespa disk-threshold patch to `0.95` was deployed; orphaned attempts 374-379 had to be failed and requeued after the `background` recreate; replacement reruns are still waiting on contextual-summary processing via LiteLLM) |
| P0 | Both | Docker Compose ports were bound to `0.0.0.0` | High blast radius. Sensitive local sandbox and MCP APIs were exposed to the local network | Keep port bindings restricted to `127.0.0.1` in compose and operator overrides | Done |
| P2 | Onyx | LiteLLM model availability was unverified | Missing Ollama models would cause LiteLLM routing failures and remove the local fallback path | Re-check `docker exec onyx-ollama-1 ollama list` after any Ollama recreate | Done |
| P1 | Onyx | Celery background workers were timing out on LiteLLM contextual-summary calls | Indexing attempts fail with `LLMTimeoutError`, leaving chunk gaps that block OpenSearch parity | `deployment/onyx/litellm_config.yaml` `router_settings.timeout` raised from 120s to 300s and `retry_after` raised from 5s to 10s; `litellm` container restarted `2026-04-30`; next pull `gemma2:27b` if needed and rerun failed indexing attempts | Done (config) — Ollama pull still needed if fallback disappears |
| P1 | Onyx | Model-server runtime env had been misaligned with the active Nomic/768 index | Any rebuild or future reindex would be unsafe if model-server env and active search settings diverged | Keep `deployment/onyx/docker-compose.yml` on `DOCUMENT_ENCODER_MODEL=nomic-ai/nomic-embed-text-v1` and `EMBEDDING_DIM=768` for both model servers; after any future recreate, verify `/api/gpu-status` and a 768-dimensional embedding response before reindex or retrieval cutover | Done |
| P1 | MCP | Consensus and Scite MCP auth flow is not wired or validated in the local proxy path | Literature validation and citation-context tools cannot be used reliably by Onyx or DeerFlow until the MCP transport and host-side auth flow are correct | Keep the proxy secret-free and do not assume static API keys for MCP. `2026-04-30` findings: Consensus MCP is an OAuth-style protected MCP resource, so an unauthenticated `curl -sS -D - -o /dev/null http://127.0.0.1:8095/consensus/sse` correctly returned `401 Unauthorized` with `WWW-Authenticate` metadata; Scite’s public MCP setup is account/sign-in based, not a local API-key injection flow, and `curl -sS -D - -o /dev/null http://127.0.0.1:8095/scite/sse` returned `404 Not Found`, so the next task is to verify the correct Scite MCP transport/path and test it through an MCP host that can complete the upstream auth flow. Do not mark Consensus operational until an MCP client completes auth and the proxy returns authenticated success instead of `401`. | Open |
| P2 | Onyx | RAG vs canonical science-file boundary had been implicit | Several retrieval-audit failures (`RAG-07`, `RAG-08`, `RAG-09`, `RAG-10`, `RAG-11`, `RAG-24`, `RAG-25`) are structural scoping failures, not model failures | Keep the boundary documented in `docs/ops/onyx-rag-optimization-2026-04-27.md` and mirror any future routing-rule changes there | Done |
| P0 | Onyx | Ollama had no models, so the LiteLLM emergency fallback was dead | `gemma2:27b` is the weight-1 fallback in the `qwen-cloud-fast` pool; without it the pool has no offline backup when Qwen quota is exhausted | Re-run `docker exec onyx-ollama-1 ollama list` after any volume reset and re-pull missing models immediately | Done |
| P1 | Onyx | Physics Validation Mode system prompt lacked RAG-vs-canon routing rules | Structural cause of `RAG-07` through `RAG-11`: agent queried `internal_search` for claim-status questions instead of reading `evidence-ledger.md` directly | Keep the source-routing block on persona id `7` and re-check after persona imports or upstream upgrades | Done |
| P1 | Onyx | Science Deep-Dive Mode was missing HEP external lookup tools | Persona id `3` had Scite/Consensus but no `hep_arxiv`, `hep_inspire`, or `hepdata` despite sharing doc sets with Physics Validation Mode | Keep tool ids `28`, `29`, and `30` attached after upgrades or persona resets | Done |
| P1 | Onyx | AiSci Wiki Agent was missing `read_file` and could not verify canon files before writing | Wiki-agent output could not be checked against `research/robert/evidence-ledger.md` | Keep tool id `9` attached to persona id `8` after upgrades or persona resets | Done |
| P1 | Onyx | Assistant persona had unanchored HEP tools with no doc sets | `hep_arxiv`, `hep_inspire`, and `hepdata` on Assistant (id `0`) created tool access with no grounding context | Keep tool ids `28`, `29`, and `30` removed from persona id `0` unless anchored doc sets are added | Done |
| P1 | Onyx | Three ghost personas remain: Draft Review Mode, Validation Orchestrator Mode, Literature Deep-Dive Mode | Unlisted personas with zero tools and zero doc sets are dead configuration that could be accidentally re-listed | Decide whether to delete them or define distinct workflows with tools and doc sets | Open |
| P1 | Onyx | Two orphaned document sets remain: Gretka-testing (`ds=2`) and Google Drive Test (`ds=4`) | Unattached sets consume connector sync cycles and pollute the admin UI | Verify no active connectors, then delete them via the Onyx admin UI | Open |
| P1 | Onyx | Vertex AI providers (`id=2`, `id=3`) are registered with no API keys | Image generation and Google Vertex AI providers fail silently; `generate_image` on Assistant is dead | Either wire the service-account JSON to providers `2` and `3` in the Onyx admin UI or disable both providers for this phase | Open |
| P2 | Onyx | `generate_image` is still exposed on Assistant with no working image backend | Tool id `2` is visible while both Vertex AI providers are unusable | Remove `generate_image` from Assistant (persona id `0`) until the provider side is actually wired, or fix the provider row above first | Open |
| P2 | Onyx | Science Deep-Dive Mode prompt lacked role differentiation from Physics Validation Mode | Users could not distinguish modes behaviorally because both prompts opened with the same AiSci guardrails block | Keep the `## Role — literature scout and source curator` header on persona id `3` after persona syncs or imports | Done |
| P2 | Onyx | `deployment/data/nginx/` is still the live runtime mount for the main web proxy | Deleting or relocating it without a compose change would break the primary Onyx nginx service, which mounts `../data/nginx` | Treat `deployment/data/nginx/` as active until a planned compose migration moves those templates under `deployment/onyx/` and updates docs together | Verified — keep |
| P2 | DeerFlow | `deployment/deer-flow/deer-flow.code-workspace` is still a live vendored root sentinel | Removing it now would break `deployment/deer-flow/skills/public/find-skills/scripts/install-skill.sh`, which walks upward looking for that file to detect project root | Replace the root-discovery sentinel during the approved de-vendoring migration, then remove the workspace file as part of that same structural change | Blocked by de-vendoring decision |
| P3 | Onyx | Embedding Exp-A (text): `BAAI/bge-m3` | `nomic-embed-text-v1` is solid, but `bge-m3` adds dense+sparse+multi-vector hybrid retrieval, higher MTEB scores, and roughly 2 GB VRAM local. Full reindex required. Dims: `1024`. | Set `DOCUMENT_ENCODER_MODEL=BAAI/bge-m3` and `EMBEDDING_DIM=1024`, create a new `search_settings` row for a parallel rebuild, then enable sparse retrieval in OpenSearch after the current cutover is stable | Planned |
| P3 | Onyx | Embedding Exp-B (visual): `nvidia/llama-nemotron-embed-vl-1b-v2` | A 1B multimodal embedding model could encode document images as vectors directly, eliminating the vision-LLM description step for figure retrieval. Full reindex and vision-pipeline changes required. | Pre-pull with `huggingface-cli download nvidia/llama-nemotron-embed-vl-1b-v2`, assess model-server compatibility, and test on an isolated connector after Exp-A | Future |
| P2 | Onyx | Contextual RAG cloud quota monitoring | `qwen3.5-omni-flash` quota is finite while contextual RAG still depends on it | Check quota monthly in DashScope. No action until the primary drops below 100K remaining requests; fallback takes over automatically | Active — monitor |

## Operational Guardrails

- [ ] Add an Onyx grounding-check gate so DeerFlow agents must retrieve a matching Onyx chunk before writing any formula or parameter claim to `research/robert/evidence-ledger.md`.
- [ ] Implement content-hash signed run artifacts so `reproducible-physics-runner` emits JSON with SHA256 of fit inputs and outputs, and `science-ledger-manager` links to the hash rather than an LLM summary.
- [ ] Wire arXiv REST API and INSPIRE-HEP as DeerFlow gateway tools after auth hardening.
- [ ] Add a model-selection-check sub-task to the DeerFlow physics agent so it computes AIC, BIC, and chi-squared per dof for every active model variant after each fit run.
- [ ] Encode pT gate values as versioned `research/robert/config/ptgates.json`, and require `reproducible-physics-runner` to load and record the applied gate values in each run artifact header.
