# Platform Backlog

Use this file for operational work on Onyx, DeerFlow, MCP, Docker, models, and deployment security. Do not put Robert's scientific claims or fit conclusions here.

| Priority | System | Issue | Why It Matters | Next Action | Status |
|---|---|---|---|---|---|
| P1 | Onyx | Active document set still has incomplete baseline and citation-context coverage | Retrieval quality is still bounded by what `HEP Phenomenology References` can ground | Split seeded sets were created and attached; HEP-native arXiv/INSPIRE/HEPData source grounding plus minimal Tsallis and Blast-Wave baseline literature are now seeded and verified for `RAG-12` through `RAG-14`; next close the remaining small-system radial-flow gap for `RAG-15` and the citation-context gap for `RAG-19` | Partially done |
| P1 | Onyx | OpenSearch retrieval cutover is not ready for the current tenant | `opensearch_document_migration_record` still has 0 rows, the tenant bookkeeping remains `PENDING`, and the active alt index is still chunk-incomplete; flipping retrieval now would move the tenant onto an unreconciled copy | Fix in dependency order — do not enable OpenSearch retrieval until all steps below pass: (1) fix the LiteLLM contextual-summary timeout so rebuild completions are no longer stalled (see Celery row); (2) fix the `KeyError: 'document_id'` OpenSearch write bug in `transformer.py` so every indexed document carries a stable `document_id`; (3) rerun the full rebuild sweep for connector/credential pairs `(11,0)`, `(13,8)`, `(14,9)`, `(10,6)`, `(4,3)`, `(1,1)`, `(7,5)`, and `(3,2)`; (4) verify `opensearch_document_migration_record` is populated and chunk parity clears via `deployment/helper/onyx_opensearch_cutover.py --json`; (5) only then flip the tenant `enable_opensearch_retrieval` flag | Blocked (`opensearch_document_migration_record` still has 0 rows; active alt index still had 37 chunk-count mismatches across 65 docs on `2026-04-28`; durable Vespa disk-threshold patch to `0.95` was deployed; orphaned attempts 374-379 had to be failed and requeued after the `background` recreate; the replacement reruns are now live but still waiting on contextual-summary processing via LiteLLM) |
| P0 | Both | Docker Compose ports are bound to 0.0.0.0 | High blast radius. Exposed highly sensitive local sandbox and MCP API to local network | Change port bindings to strictly use 127.0.0.1: | Done |
| P2 | Onyx | Unverified LiteLLM Model Status | If all models are not pulled, LiteLLM routing will fail with 500s | Check Ollama container to confirm gemma2:27b and qwen2.5vl:7b are cached | Done |
| P1 | Onyx | Celery Background Workers Timing Out on LiteLLM | Indexing attempts fail with `LLMTimeoutError` because local 32b models take too long to generate contextual summaries, missing 37 chunks and blocking the rebuild sweep from reaching parity | Update `deployment/onyx/litellm_config.yaml` to use a 300s timeout for the contextual-summary path; verify the runtime config picks it up; rerun failed indexing attempts only after at least one healthy local/chat Ollama path is available so the timeout increase can actually unblock completion rather than just extending the stall | Open |
| P1 | Onyx | Model-server runtime env is misaligned with the active Nomic/768 index | Any rebuild or future reindex will be unsafe while `inference_model_server` and `indexing_model_server` still expose `DOCUMENT_ENCODER_MODEL=sentence-transformers/all-MiniLM-L6-v2` and `EMBEDDING_DIM=384` against an active `nomic-ai/nomic-embed-text-v1` 768-dimensional search configuration; GPU passthrough is also absent despite the host RTX 3090 | Update `deployment/onyx/docker-compose.yml` so both model servers set `DOCUMENT_ENCODER_MODEL=nomic-ai/nomic-embed-text-v1` and `EMBEDDING_DIM=768`; add working GPU device exposure for `inference_model_server`, `indexing_model_server`, and `ollama`; after approval, recreate only those containers and verify `/api/gpu-status` returns `gpu_available: true` and the embedding endpoint produces 768-dimensional vectors before any reindex or retrieval cutover work | Open |
| P1 | MCP | Scite and Consensus MCP proxy keys are not configured or tested | Literature validation and citation context tools cannot be used reliably by Onyx or DeerFlow until the proxy is wired | Add Scite and Consensus API keys to the local `nginx_mcp_proxy` configuration (not committed), reload Nginx, run a small test query for each service, and record the outcome and example commands here. | Open |
| P2 | Onyx | RAG vs canonical science-file boundary was implicit rather than explicit | Several retrieval-audit failures (`RAG-07`, `RAG-08`, `RAG-09`, `RAG-10`, `RAG-11`, `RAG-24`, `RAG-25`) are structural scoping failures, not model failures; without an explicit rule, agents may route science-gate or claim-status questions through RAG instead of reading from `research/robert/*` canon | Documented the boundary in `docs/ops/onyx-rag-optimization-2026-04-27.md` as a question-family table; added the ordered OpenSearch and model-server fix plan to the same file | Done |
