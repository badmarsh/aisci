# Platform Backlog

Use this file for operational work on Onyx, DeerFlow, MCP, Docker, models, and deployment security. Do not put Robert's scientific claims or fit conclusions here.

| Priority | System | Issue | Why It Matters | Next Action | Status |
|---|---|---|---|---|---|
| P0 | DeerFlow | Live secret values are reachable through local MCP config API | Unsafe if DeerFlow is exposed beyond localhost | Keep localhost-only; add auth/redaction before LAN/remote exposure | Open |
| P0 | DeerFlow | Docker socket and CLI auth dirs mounted into containers | High blast radius if gateway is exposed | Restrict network exposure and document operational assumptions | Open |
| P1 | DeerFlow | Agent roster is not physics-oriented | Agents plan around wrong domain roles | Replaced with physics validation agents in live config | Done |
| P1 | DeerFlow | Broken `rag_search`, `rag_manage`, `llm_council` tools | Agents may call unavailable modules | Removed from active config until implemented | Done |
| P1 | DeerFlow | MCP paths reference missing `/home/ubuntu/deer-flow` | Filesystem/sqlite MCP tools can fail | Switched to container paths and `/workspace/aisci` | Done |
| P1 | DeerFlow | Missing Scite/Consensus/arXiv/INSPIRE integrations | Literature workflow not fully automated | Add after auth/path testing | Open |
| P1 | DeerFlow | Default model used Vertex without visible credentials | Default runs may fail | OpenRouter Gemini Flash is now first model | Done |
| P1 | Onyx | Physics persona has no tools/document sets | Core validation persona is nonfunctional | Live `Physics Validation Mode` now has Physics, Robert Boson Draft, HEP Phenomenology References, internal search, file reader, Python, URL opening, Scite, Consensus, arXiv, INSPIRE-HEP, and HEPData attached | Done |
| P1 | Onyx | Active document set still has incomplete baseline and citation-context coverage | Retrieval quality is still bounded by what `HEP Phenomenology References` can ground | Split seeded sets were created and attached; HEP-native arXiv/INSPIRE/HEPData source grounding plus minimal Tsallis and Blast-Wave baseline literature are now seeded and verified for `RAG-12` through `RAG-14`; next close the remaining small-system radial-flow gap for `RAG-15` and the citation-context gap for `RAG-19` | Partially done |
| P1 | Onyx | Missing arXiv/INSPIRE-HEP/HEPData tools | Physics personas cannot directly verify HEP identifiers, data tables, or citation records | Read-only custom OpenAPI tools were installed and attached to `Physics Validation Mode`; selected ATLAS 13 TeV arXiv/INSPIRE/HEPData records were seeded into `HEP Phenomenology References` | Done |
| P1 | Onyx | Runtime model-server stack still does not match the active Nomic 768 index | Reindexing or retrieval experiments can drift onto the wrong embedding shape and the local servers cannot use the host GPU yet | Keep Vespa active; expose GPUs to `inference_model_server`, `indexing_model_server`, and `ollama`, then make the runtime model-server env match the active `nomic-ai/nomic-embed-text-v1` / `768` search settings before any reindex | Done |
| P1 | Onyx | OpenSearch retrieval cutover is not ready for the current tenant | Enabling the tenant flag now would move retrieval onto an index copy whose migration metadata is still `PENDING`/empty and whose active alt index is missing chunks for several indexed docs, including the HEPData and baseline-literature sources used in the physics RAG set | Keep Vespa active; repair/populate `opensearch_*_migration_record`, reconcile active alt-index chunk parity against DB `chunk_count`, and clear or account for stale primary-index docs before enabling tenant retrieval | Open |
| P2 | DeerFlow | Better Auth base URL missing | Redirects/callbacks may fail | Set local frontend auth URL vars | Done |
| P2 | DeerFlow | LangSmith metadata 403 noise | Logs hide real problems | Cleared placeholder LangSmith key and disabled tracing | Done |
| P2 | Onyx | Stale Compose labels point to old path | Confusing operations/debugging | Recreate stack from `/home/ubuntu/aisci/deployment/onyx` | Done |
| P2 | Onyx | Local Unstructured API is configured but not actually used by the current backend path | PDF parsing can silently fall back to built-in parsers even though the local Unstructured container is healthy | Patch the Onyx backend so `UnstructuredClient` receives `server_url=http://unstructured:8000` and the extraction gate does not require a cloud-style API key for local use; only then recreate `api_server` and `background` | Done |
| P2 | Onyx | LiteLLM and Ollama model stack is unhealthy | Chat, fallback, and API-based embedding/rerank paths are effectively unusable while LiteLLM health is `0/7` and Ollama has no models | Pulled Ollama models and disabled dead aliases | Done |
| P2 | Onyx | Tsallis connector still emits stale OpenSearch no-op and missing-chunk noise | Log noise now overlaps with a real alt-index chunk-parity problem and can mask fresh indexing failures | Documented chunk parity gap caused by failed OpenSearch writes missing chunk 4. | Done |
| P2 | Onyx | Background process monitor still reports false positives | Operational noise makes healthy workers look broken even when queues stay at zero | Keep the workers as-is; tighten the process-classification logic behind `monitor_process_memory` so the tail/log helper process is not misclassified as the canonical `beat` process | Done |
| P2 | Both | Retrieval/evaluation question set was missing | Tuning is guesswork without a baseline run | Draft set added in `docs/ops/onyx-rag-optimization-2026-04-27.md`; run it and record observed citations, latency, and failure modes before tuning | Done |
